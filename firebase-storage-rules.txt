// Development-only Firebase Storage rules (VERY OPEN)
// WARNING: These rules are intentionally permissive for local testing and
// troubleshooting (e.g., media loading). Do NOT deploy these to production.
// This file is a local document only and does not change real deployed rules.

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Development: allow public read and public write to all paths.
    // Extremely permissive — suitable only for local/dev debugging.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }

  }
}
