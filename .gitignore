# Ignore archived raw JSON shards (we keep a compressed tarball instead)
/data/archives/*.json

# Canonical merged databases (avoid adding very large files to repo)
/data/master_pois_database.json
/data/master_locations_database.json

# Node / editor
node_modules/
.vscode/.vercel
.env*

# Generated POI and large DB files
pois/places.json
pois/search-index.json
pois/states/
pois/tiles/
tmp/
*.bak
master_pois_database.json
master_locations_database.json
!data/samples/master_pois_database.json
data/archives/
# Generator artifact (data is merged into poi_database_worldwide.json)
poi_database_states.json

# Obsolete: replaced by /pois/* shards (per-state lazy load)
poi_database_worldwide.json

# Obsolete: replaced by /pois/* shards (per-state lazy load)
poi_database_worldwide.json

# OSM fetcher log (regenerated each run)
pois/osm/_fetch.log

# Raw OSM fetch cache (data is merged into pois/states/*.json shards)
pois/osm/

# Node
node_modules/
npm-debug.log

# Local archive for moved large assets
Fedex_archives/

# Service account keys / secrets (NEVER commit)
serviceAccountKey.json
*serviceAccount*.json
*-vision-key.json
secrets/
.vercel

# Local Safari-like HTTPS testing artifacts
.certs/
.run/
