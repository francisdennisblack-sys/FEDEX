// firebase-db.js: stubbed during cleanup. Firebase logic is now inlined in `index.html`.
// Keeping a small stub here so builds that import the module won't fail. Remove import usages instead.
export const database = null;
export const storage = null;
export const app = null;

