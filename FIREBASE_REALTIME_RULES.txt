{
  "rules": {
    // Online users tracking - ANYONE can read and write (MVP testing)
    "onlineUsers": {
      "$uid": {
        ".read": true,
        ".write": true
      }
    },
    
    // Public posts - ANYONE can read and write (MVP testing)
    "posts": {
      "$postId": {
        ".read": true,
        ".write": true
      }
    },
    
    // Zone-based posts index - ANYONE can read and write (MVP testing)
    "posts_by_zone": {
      "$zone": {
        "$postId": {
          ".read": true,
          ".write": true
        }
      }
    },
    
    // User data - only owners can write
    "users": {
      "$uid": {
        ".read": true,
        ".write": true,
        
        // User's posts index
        "posts": {
          "$postId": {
            ".read": true,
            ".write": true
          }
        },
        
        // User profile
        "profile": {
          ".read": true,
          ".write": true
        },
        
        // User stats
        "stats": {
          ".read": true,
          ".write": true
        },
        
        // Location history
        "locationHistory": {
          ".read": true,
          ".write": true
        }
      }
    },
    
    // Deny all other paths
    ".read": false,
    ".write": false
  }
}
